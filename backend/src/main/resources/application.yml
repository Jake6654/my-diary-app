spring:
  application:
    name: diary-backend

  datasource:
    # ⬇️ Supabase가 보여준 Session pooler 연결 문자열 기준
    url: jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:5432/postgres?sslmode=require
    username: postgres.eensskjncsooyuxbfokm # Supabase에서 복사한 사용자명
    password: Lakezzang123@ # 네 DB 비밀번호
    driver-class-name: org.postgresql.Driver

    hikari:
      maximum-pool-size: 3 # 🔥 핵심: 풀 사이즈 확 줄이기
      minimum-idle: 1
      idle-timeout: 30000 # 30초
      connection-timeout: 20000 # 20초

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

ai:
  flux:
    base-url: "http://34.21.94.52:8000"

server:
  port: ${PORT:8080}
