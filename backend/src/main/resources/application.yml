spring:
  application:
    name: diary-backend

  datasource:
    datasource:
      url: jdbc:postgresql://db.eensskjncsooyuxbfokm.supabase.co:5432/postgres?user=postgres&password=Lakezzang123@&sslmode=require
      driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

