spring:
  application:
    name: diary-backend

  datasource:
    # ✅ Supabase Direct connection (Database 탭 → Direct connection 에서 복사)
    url: jdbc:postgresql://db.eensskjncsooyuxbfokm.supabase.co:5432/postgres?sslmode=require
    username: postgres # ⬅ 이거 중요! (pooler 계정 아님)
    password: Lakezzang123@ # 네 DB 비번
    driver-class-name: org.postgresql.Driver

    hikari:
      maximum-pool-size: 5 # Free/소규모에 충분
      minimum-idle: 1
      idle-timeout: 30000
      connection-timeout: 20000

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

ai:
  flux:
    base-url: "http://34.21.94.52:8000"

server:
  port: ${PORT:8080}
